c Tests that the keyboard compiles without warnings with 14.0 touchlayout special keys.

VERSION 9.0
store(&TARGETS) 'web'
store(&LAYOUTFILE) 'test_touchlayout_14_3.keyman-touch-layout'

begin Unicode > use(main)

group(main) using keys
