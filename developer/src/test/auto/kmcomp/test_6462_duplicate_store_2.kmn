c test_6462_duplicate_store_2

store(&VERSION) '10.0'
store(&TARGETS) 'windows'

store(&NAME) "My Keyboard"

store(&name) "Still My Keyboard"

begin Unicode > use(main)

group(main) using keys

+ [K_A] > 'ah'
