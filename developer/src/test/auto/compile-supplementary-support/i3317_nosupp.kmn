c This should not generate KS=1 in the .js
store(&VERSION) '9.0'
store(&NAME) 'I3317 - no supp chars'
store(&TARGETS) 'web'

begin Unicode > use(main)

store(something) 'abc'

group(main) using keys

'abc' + 'd' > 'efg'

match > 'abc'
nomatch > 'abc'
