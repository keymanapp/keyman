c This should generate KS=1 in the .js
store(&VERSION) '9.0'
store(&NAME) 'I3317 - with supp chars'
store(&TARGETS) 'web'

begin Unicode > use(main)



store(something) 'abc' U+13000

group(main) using keys

 'abc' + 'd' > 'efg'

match > 'abc'
nomatch > 'abc'
