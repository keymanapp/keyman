import 'mocha';
import { assert } from 'chai';
//import { ListIndex, ListItem } from '../../src/ldml-keyboard/string-list.js';

describe('Test of String-List', () => {
  describe('should test ultimate truth', () => {
    it('the truth', () => {
      assert.isTrue(true);
    });
  });
});