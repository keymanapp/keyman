#pragma once

KMX_DWORD VerifyCasedKeys(PFILE_STORE sp);
KMX_DWORD ExpandCapsRulesForGroup(PFILE_KEYBOARD fk, PFILE_GROUP gp);

KMX_DWORD KMX_VerifyCasedKeys(PFILE_STORE sp);
KMX_DWORD KMX_ExpandCapsRulesForGroup(PFILE_KEYBOARD fk, PFILE_GROUP gp);
