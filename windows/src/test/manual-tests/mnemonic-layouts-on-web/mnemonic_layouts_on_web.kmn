store(&NAME) 'Mnemonic Layout on Web'
store(&TARGETS) 'any windows'
store(&mnemoniclayout) '1'
store(&LAYOUTFILE) 'mnemonic_layouts_on_web-layout.js'

begin Unicode > use(main)

group(main) using keys

+ [T_M] > 'MMM'
