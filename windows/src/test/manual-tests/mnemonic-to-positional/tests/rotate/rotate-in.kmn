store(&VERSION) '9.0'
store(&NAME) 'Rotate-in'
store(&mnemoniclayout) '1'

begin Unicode > use(main)


group(main) using keys

+ 'q' > 'the-q-key'
+ 'w' > 'the-w-key'
+ 'e' > 'the-e-key'
+ 'a' > 'the-a-key'
